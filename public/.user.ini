; 업로드 한도 (상세설명 에디터 이미지 붙여넣기 등) — PHP-FPM/CGI 적용
; 실제 상한은 nginx client_max_body_size(50M)가 결정. 아래는 PHP 여유값.
upload_max_filesize = 60M
post_max_size = 64M
memory_limit = 256M
max_execution_time = 120
max_input_time = 120
